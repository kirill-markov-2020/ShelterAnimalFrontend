import React from 'react';
import { Container } from '@mui/material';
import ShelterInfo from '../components/ShelterInfo';

const ShelterInfoPage: React.FC = () => {
  return (
    <Container>
      <ShelterInfo />
    </Container>
  );
};

export default ShelterInfoPage;